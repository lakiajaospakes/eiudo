const animationDelay = Math.random() * 5; // Generate a random number between 0 and 5
const delayInSeconds = `${animationDelay}s`; // Add the "s" unit to represent seconds

console.log(delayInSeconds); // Output: "3.456s" (example random value)
